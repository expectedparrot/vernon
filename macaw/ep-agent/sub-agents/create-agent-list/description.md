Create AgentLists from web searches, descriptions, local files, or programmatic generation. Use when the user wants to build an AgentList from any source including CSV, Excel, dictionaries, DataFrames, web research, or combinatorial generation. Ask the user questions to gather enough context before delegating to the sub-agent. At a minimum, ask the user how many agents they want to create and whether they want to push the resulting AgentList to coop (Expected Parrot's servers).
