[project]
name = "agent-list-builder"
version = "0.1.0"
description = "Claude Agent SDK sub-agent for creating EDSL AgentLists"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "edsl",
]

[project.scripts]
agent-list-builder = "agent:main"
